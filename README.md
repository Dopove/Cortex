# Cortex
Cortex: Mojo-powered multi-agent AI bundler &amp; runtime. Build once, run everywhere with Turbo Mode for 3-5x parallel speedup. Hardware-aware quantization, shared KV cache, &lt;5s cold start. Bundle &amp; execute CrewAI projects as native .cortex files.
