This file is a merged representation of a subset of the codebase, containing files not matching ignore patterns, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of a subset of the repository's contents that is considered the most important context.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching these patterns are excluded: modern_env/
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
knowledge/
  user_preference.txt
src/
  scrapper/
    config/
      __init__.py
      agents.yaml
      crew.yaml
      tasks.yaml
    tools/
      __init__.py
      custom_tool.py
      dataset_tools.py
      huggingface_downloader.py
      image_processing_tools.py
      kaggle_downloader.py
      llama_tools.py
      metadata_tools.py
      scrape_tools.py
      search_tools.py
      url_validator.py
    utils/
      data_export.py
      duplicate_checker.py
    crew.py
    main.py
  fashion_data_20250902_001145.tsv
  fashion_data_20250902_001150.tsv
  recovery.py
!LOGFILE!
.gitignore
pyproject.toml
README.md
requirements.txt
run_overnight.bat
test.py
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="knowledge/user_preference.txt">
User name is John Doe.
User is an AI Engineer.
User is interested in AI Agents.
User is based in San Francisco, California.
</file>

<file path="src/scrapper/config/__init__.py">
"""
Configuration for Fashion Data Scraper
"""
import os
from pathlib import Path

# Base paths - using your exact directory structure
BASE_DATA_DIR = Path(r"C:/Users/saran/Videos/Projects/FDM/dataCollector")

# Directory structure
COLLECTED_DATA_DIR = BASE_DATA_DIR / "collected_data"
DUPLICATE_TRACKING_DIR = BASE_DATA_DIR / "duplicate_tracking"
LOGS_DIR = BASE_DATA_DIR / "logs"
METADATA_DIR = BASE_DATA_DIR / "metadata"
PROCESSED_DATA_DIR = BASE_DATA_DIR / "processed_data"

# Ollama/LLaMA Configuration
OLLAMA_BASE_URL = "http://localhost:11434"
OLLAMA_MODEL = "llama3.1:8b"
LLAMA_MAX_TOKENS = 200
LLAMA_TEMPERATURE = 0.3

# Fashion Keywords for Search
FASHION_KEYWORDS = {
    "theory": [
        "fashion design principles", "color theory fashion", "garment construction",
        "fashion history", "textile properties", "draping techniques",
        "pattern making", "fashion sketching", "silhouette design"
    ],
    "trends": [
        "fashion trends 2025", "runway fashion", "street style trends",
        "color forecasting", "seasonal fashion", "fashion week reports",
        "consumer fashion preferences", "fashion market analysis"
    ],
    "visual": [
        "fashion sketches", "technical drawings fashion", "garment patterns",
        "textile designs", "fabric swatches", "fashion illustrations",
        "clothing photography", "runway photos", "fashion editorial"
    ],
    "academic": [
        "fashion research papers", "textile engineering", "fashion psychology",
        "sustainable fashion", "fashion marketing", "apparel manufacturing"
    ]
}

# Storage structure mapping - matches your folder structure exactly
STORAGE_STRUCTURE = {
    "structured": {
        "market_data": "collected_data/structured/market_data",
        "product_catalogs": "collected_data/structured/product_catalogs",
        "trend_metrics": "collected_data/structured/trend_metrics"
    },
    "textual": {
        "academic_papers": "collected_data/textual/academic_papers",
        "consumer_feedback": "collected_data/textual/consumer_feedback",
        "fashion_theory": "collected_data/textual/fashion_theory",
        "trend_reports": "collected_data/textual/trend_reports"
    },
    "visual": {
        "garment_images": "collected_data/visual/garment_images",
        "patterns": "collected_data/visual/patterns",
        "sketches": "collected_data/visual/sketches",
        "technical_drawings": "collected_data/visual/technical_drawings"
    }
}

# Quality Score Thresholds
MIN_QUALITY_SCORES = {
    "fashion_llm": 7,
    "classifier": 8,
    "sketch_gen": 6,
    "pattern_gen": 7
}

def create_directories():
    """Create directory structure matching your existing folders"""
    # Base directories
    directories = [
        COLLECTED_DATA_DIR,
        DUPLICATE_TRACKING_DIR, 
        LOGS_DIR,
        METADATA_DIR,
        PROCESSED_DATA_DIR
    ]
    
    # Add all subdirectories from STORAGE_STRUCTURE
    for category, subcategories in STORAGE_STRUCTURE.items():
        for subcategory, relative_path in subcategories.items():
            full_path = BASE_DATA_DIR / relative_path
            directories.append(full_path)
    
    # Create all directories
    for directory in directories:
        directory.mkdir(parents=True, exist_ok=True)
    
    print(f"[SUCCESS] Created directory structure at {BASE_DATA_DIR}")
    print(f"   - structured: market_data, product_catalogs, trend_metrics")
    print(f"   - textual: academic_papers, consumer_feedback, fashion_theory, trend_reports")
    print(f"   - visual: garment_images, patterns, sketches, technical_drawings")

# Export all configuration
__all__ = [
    "create_directories", "OLLAMA_BASE_URL", "OLLAMA_MODEL", 
    "LLAMA_MAX_TOKENS", "LLAMA_TEMPERATURE", "FASHION_KEYWORDS",
    "STORAGE_STRUCTURE", "BASE_DATA_DIR", "COLLECTED_DATA_DIR",
    "DUPLICATE_TRACKING_DIR", "LOGS_DIR", "METADATA_DIR", "PROCESSED_DATA_DIR"
]
</file>

<file path="src/scrapper/config/agents.yaml">
# agents.yaml
# Defines the complete configuration for each agent in the fashion data collection crew.

discovery_specialist:
  role: 'Fashion URL Discovery Specialist'
  goal: 'Discover a diverse set of high-quality fashion URLs from various online sources, including blogs, news sites, and e-commerce platforms, and validate their relevance and accessibility.'
  backstory: 'You are an expert digital librarian with a keen eye for fashion. Your mission is to build a comprehensive list of web sources that will serve as the foundation for a rich and varied dataset. You are meticulous about verifying that every URL is active and on-topic.'
  # Tools assigned directly in the agent's configuration
  tools:
    - FashionURLDiscoveryTool
    - URLValidatorTool
  verbose: true
  allow_delegation: false

scraping_specialist:
  role: 'Web Scraping Specialist'
  # FIX: More direct and explicit goal to guide the LLM
  goal: "Your only goal is to use the 'BatchScraping' tool. Take the list of URLs from the previous step and use this tool to extract the content from each one."
  # FIX: Reinforced backstory to prevent inventing actions
  backstory: >
    You are a specialized bot with one function: to operate the 'BatchScraping' tool.
    You do not deviate, you do not interpret, and you do not use any action name other than 'BatchScraping'.
    Your task is to take a list of URLs and return the raw scraped content.
  tools:
    - BatchScrapingTool
    - PhotoToSketchTool
  verbose: true
  allow_delegation: false

quality_assessor:
  role: 'Fashion Content Quality Assessor'
  goal: 'Your only goal is to use your tools to analyze a piece of text. You must return only the raw, structured JSON output from each tool you use.'
  backstory: >
    You are a data processing API. You do not write prose, you do not explain yourself, and you do not add any text before or after the JSON output.
    You receive text, apply a tool, and output ONLY the resulting JSON data. You are a machine. Behave like one.
  tools:
    - LlamaQualityAssessmentTool
    - LlamaRelevanceFilterTool
    - FashionMetadataExtractor
  verbose: true
  allow_delegation: false

content_categorizer:
  role: 'Fashion Content Categorizer'
  goal: 'Organize and label all high-quality content into a precise, hierarchical folder structure to make it useful for model training.'
  backstory: 'You are an AI-powered archivist. You understand the nuances of fashion and can instantly determine if a piece of content is about "fashion theory," a "trend report," or a specific "garment." Your categorization is flawless and essential for the success of the project.'
  tools:
    - LlamaCategorizationTool
  verbose: true
  allow_delegation: false

dataset_specialist:
  role: 'Fashion Dataset Specialist'
  goal: 'Download and preprocess large-scale fashion datasets from established repositories like HuggingFace and Kaggle.'
  backstory: 'You are an expert in data engineering, specializing in acquiring and preparing massive datasets for AI training. You are familiar with the APIs and file structures of all major data repositories.'
  verbose: true
  allow_delegation: false
</file>

<file path="src/scrapper/config/crew.yaml">
crew:
  agents:
    - discovery_specialist
    - scraping_specialist  
    - quality_assessor
    - content_categorizer
  tasks:
    - url_discovery
    - content_scraping
    - quality_assessment
    - content_categorization
  process: sequential
  verbose: true
</file>

<file path="src/scrapper/config/tasks.yaml">
# tasks.yaml

url_discovery:
  description: 'Generate a list of at least 15 URLs related to emerging fashion trends for 2025. The list must include a mix of high-fashion blogs, runway reviews, and street style galleries. Validate each URL to ensure it returns a 200 OK status.'
  agent: discovery_specialist
  expected_output: 'A clean JSON list of validated URLs.'

content_scraping:
  # FIX: A direct, unambiguous command to the agent
  description: >
    Your task is to execute the 'BatchScraping' tool.
    Use the list of validated URLs from the previous task as the input for the 'urls' parameter of the 'BatchScraping' tool.
    Do not perform any other action.
  agent: scraping_specialist
  expected_output: 'The raw output from the "BatchScraping" tool, which will be a JSON object containing the scraped content.'

quality_assessment:
  description: 'Execute the LlamaQualityAssessmentTool on the provided text. Then, execute the FashionMetadataExtractorTool. Return the combined JSON from both tools.'
  agent: quality_assessor
  expected_output: 'A single JSON object containing the combined results from the quality assessment and metadata extraction, including keys like "quality_score", "brands", and "styles".'

content_categorization:
  description: 'Take the quality-assessed and enriched data. For each item, determine the most appropriate sub-folder within the "collected_data" directory and assign its category.'
  agent: content_categorizer
  expected_output: 'The final JSON data structure, with each item including a "category" key pointing to its correct storage path (e.g., "textual/trend_reports").'

dataset_collection:
  description: "Download the 'zalando-research/fashion-mnist' dataset from Kaggle and the 'fashion_mnist' dataset from HuggingFace. Prepare them for training."
  agent: dataset_specialist
  expected_output: "Confirmation that the datasets have been downloaded and are ready in the appropriate directory."
</file>

<file path="src/scrapper/tools/custom_tool.py">
from typing import Type
from pydantic import BaseModel, Field


class MyCustomToolInput(BaseModel):
    """Input schema for MyCustomTool."""
    argument: str = Field(..., description="Description of the argument.")

class MyCustomTool:
    name: str = "Name of my tool"
    description: str = (
        "Clear description for what this tool is useful for, your agent will need this information to use it."
    )
    args_schema: Type[BaseModel] = MyCustomToolInput

    def _run(self, argument: str) -> str:
        # Implementation goes here
        return "this is an example of a tool output, ignore it and move along."
</file>

<file path="src/scrapper/tools/dataset_tools.py">
# scrapper/tools/dataset_tools.py

from pydantic import BaseModel, Field, ConfigDict
from crewai.tools import BaseTool
from typing import Type, Dict, Any, List, Optional
from datetime import datetime
from pathlib import Path
import json
import subprocess  # <-- FIX: Added missing import

try:
    from datasets import load_dataset
    DATASETS_AVAILABLE = True
except ImportError:
    load_dataset = None
    DATASETS_AVAILABLE = False

class KaggleDatasetArgs(BaseModel):
    dataset_name: str = Field(..., description="Kaggle dataset name")
    model_config = ConfigDict(extra="allow")

class KaggleDatasetTool(BaseTool):
    # FIX: Added ': str' type annotation
    name: str = "KaggleDatasetDownloader"
    description: str = "Download and unzip fashion datasets from Kaggle."
    args_schema: Type[BaseModel] = KaggleDatasetArgs

    def _run(self, dataset_name: str, **kwargs) -> Dict[str, Any]:
        output_path = "C:/Users/saran/Videos/Projects/FDM/dataCollector/structured/kaggle_datasets"
        # ... (rest of the Kaggle tool code is correct) ...
        # NOTE: The rest of your Kaggle tool code was already robust.
        # The only required fix was the type annotation on 'name' and 'description'.
        try:
            output_dir = Path(output_path)
            output_dir.mkdir(parents=True, exist_ok=True)
            
            try:
                subprocess.run(["kaggle", "--version"], capture_output=True, check=True, text=True)
            except (subprocess.CalledProcessError, FileNotFoundError):
                return {"success": False, "error": "Kaggle CLI not found."}
            
            cmd = ["kaggle", "datasets", "download", "-d", dataset_name, "-p", str(output_path), "--unzip"]
            result = subprocess.run(cmd, capture_output=True, text=True, timeout=300)
            
            if result.returncode == 0:
                return {"success": True, "message": f"Dataset {dataset_name} downloaded."}
            else:
                return {"success": False, "error": result.stderr or result.stdout}
        except Exception as e:
            return {"success": False, "error": str(e)}


class HuggingFaceDatasetArgs(BaseModel):
    dataset_name: str = Field(..., description="HuggingFace dataset name")
    split: str = Field(default="train", description="Dataset split to download")
    max_samples: int = Field(default=1000, description="Maximum samples to save")
    model_config = ConfigDict(extra="allow")

class HuggingFaceDatasetTool(BaseTool):
    # FIX: Added ': str' type annotation
    name: str = "HuggingFaceDatasetDownloader"
    description: str = "Download datasets from the HuggingFace Hub."
    args_schema: Type[BaseModel] = HuggingFaceDatasetArgs

    def _run(self, dataset_name: str, split: str = "train", max_samples: int = 1000, **kwargs) -> Dict[str, Any]:
        if not DATASETS_AVAILABLE:
            return {"success": False, "error": "'datasets' library not installed."}

        try:
            dataset = load_dataset(dataset_name, split=split, streaming=True) # type: ignore
            output_dir = Path("C:/Users/saran/Videos/Projects/FDM/dataCollector/structured/huggingface_datasets")
            output_dir.mkdir(parents=True, exist_ok=True)
            dataset_file = output_dir / f"{dataset_name.replace('/', '_')}_{split}.jsonl"

            count = 0
            with open(dataset_file, "w", encoding="utf-8") as f:
                for item in dataset:
                    if count >= max_samples: break
                    f.write(json.dumps(item, ensure_ascii=False) + "\\n")
                    count += 1

            return {"success": True, "message": f"Saved {count} samples to {dataset_file}."}
        except Exception as e:
            return {"success": False, "error": str(e)}
</file>

<file path="src/scrapper/tools/huggingface_downloader.py">
#type: ignore
import numpy as np
import json
import os
from datasets import load_dataset

class HuggingFaceDatasetDownloader:
    def __init__(self):
        self.base_dir = "C:/Users/saran/Videos/Projects/FDM/dataCollector/structured"
    
    def download_fashion_mnist(self, split="train", max_samples=1000):
        try:
            dataset = load_dataset("fashion_mnist")
            
            os.makedirs(f"{self.base_dir}/fashion_mnist_hf", exist_ok=True)
            
            images = []
            labels = []
            
            for i, item in enumerate(dataset[split]):
                if i >= max_samples:
                    break
                    
                # Convert PIL Image to numpy array then to list for JSON serialization
                img_array = np.array(item["image"])
                images.append(img_array.tolist())
                labels.append(item["label"])
            
            # Save as JSON files instead of trying to serialize PIL images
            with open(f"{self.base_dir}/fashion_mnist_hf/{split}_images.json", "w") as f:
                json.dump(images, f)
            
            with open(f"{self.base_dir}/fashion_mnist_hf/{split}_labels.json", "w") as f:
                json.dump(labels, f)
                
            return {
                "success": True,
                "message": f"Downloaded {len(images)} samples from {split} split"
            }
            
        except Exception as e:
            return {
                "success": False,
                "error": str(e)
            }
</file>

<file path="src/scrapper/tools/image_processing_tools.py">
# scrapper/tools/image_processing_tools.py

from pydantic import BaseModel, Field, ConfigDict
from crewai.tools import BaseTool
from typing import Type, Dict, Any, Optional
from datetime import datetime
from pathlib import Path

# Make sure you have Pillow installed: pip install Pillow
try:
    from PIL import Image, ImageFilter
except ImportError:
    raise ImportError("Pillow library not found. Please install it with 'pip install Pillow'")

class PhotoToSketchArgs(BaseModel):
    """Arguments for the PhotoToSketchTool."""
    image_path: str = Field(..., description="The absolute file path to the input image that needs to be converted to a sketch.")
    output_path: Optional[str] = Field(None, description="Optional custom file path for saving the output sketch image.")

    # FIX: Allows the tool to accept internal context fields from CrewAI without crashing.
    model_config = ConfigDict(extra="allow")

class PhotoToSketchTool(BaseTool):
    """
    A tool that converts a given fashion photograph into a digital line-art sketch.
    It takes a file path for an image, processes it to create a sketch effect,
    and saves the resulting sketch to the designated 'sketches' directory.
    """
    name: str = "PhotoToSketch"
    description: str = "Converts a fashion photo from a file path into a digital sketch."
    args_schema: Type[BaseModel] = PhotoToSketchArgs

    def _run(self, image_path: str, output_path: Optional[str] = None, **kwargs) -> Dict[str, Any]:
        """
        Executes the photo-to-sketch conversion.
        """
        try:
            input_file = Path(image_path)
            if not input_file.is_file():
                return {"success": False, "error": f"Input image not found or is not a file at path: {image_path}"}

            # Open the image using Pillow
            with Image.open(input_file) as img:
                # Convert the image to grayscale for a classic sketch look
                grayscale_img = img.convert("L")
                
                # Apply a contour filter to find edges, creating a sketch-like effect
                sketch_img = grayscale_img.filter(ImageFilter.CONTOUR)

                # Determine the output path based on the project structure
                if output_path:
                    # Use the provided output path but ensure it's just the filename
                    output_filename = Path(output_path).name
                else:
                    # Create a descriptive, unique filename if none is provided
                    timestamp = datetime.now().strftime('%Y%m%d_%H%M%S')
                    output_filename = f"sketch_{input_file.stem}_{timestamp}.png"

                # Define the correct output directory from your project plan
                output_dir = Path("C:/Users/saran/Videos/Projects/FDM/dataCollector/collected_data/visual/sketches")
                output_dir.mkdir(parents=True, exist_ok=True)
                full_output_path = output_dir / output_filename
                
                # Save the final sketch image
                sketch_img.save(full_output_path)

                print(f"✅ Sketch successfully generated and saved to: {full_output_path}")
                return {
                    "success": True,
                    "input_path": image_path,
                    "output_path": str(full_output_path),
                    "processed_at": datetime.now().isoformat()
                }

        except Exception as e:
            # Catch any other errors during processing (e.g., corrupted image file)
            return {"success": False, "error": f"An unexpected error occurred during image processing: {str(e)}", "input_path": image_path}
</file>

<file path="src/scrapper/tools/kaggle_downloader.py">
import requests
import os

class KaggleDatasetDownloader:
    def __init__(self):
        self.base_dir = "C:/Users/saran/Videos/Projects/FDM/dataCollector/structured"
    
    def download_fashion_mnist_direct(self):
        """Direct download without Kaggle API"""
        base_url = "http://fashion-mnist.s3-website.eu-central-1.amazonaws.com/"
        files = [
            "train-images-idx3-ubyte.gz",
            "train-labels-idx1-ubyte.gz", 
            "t10k-images-idx3-ubyte.gz",
            "t10k-labels-idx1-ubyte.gz"
        ]
        
        os.makedirs(f"{self.base_dir}/fashion_mnist", exist_ok=True)
        
        for file in files:
            url = base_url + file
            response = requests.get(url)
            if response.status_code == 200:
                with open(f"{self.base_dir}/fashion_mnist/{file}", "wb") as f:
                    f.write(response.content)
                print(f"✅ Downloaded: {file}")
            else:
                print(f"❌ Failed to download: {file}")
</file>

<file path="src/scrapper/tools/llama_tools.py">
# scrapper/tools/llama_tools.py

from pydantic import BaseModel, Field, ConfigDict
from crewai.tools import BaseTool
from typing import Type, Dict, Any
from datetime import datetime
import json
from pathlib import Path

# ------------------------------
# --- Pydantic Argument Models ---
# ------------------------------

class QualityAssessmentArgs(BaseModel):
    content: str = Field(..., description="The content to be assessed for quality.")
    # FIX: Use 'allow' to be compatible with CrewAI's internal context passing
    model_config = ConfigDict(extra='allow')

class RelevanceFilterArgs(BaseModel):
    content: str = Field(..., description="The content to be filtered for relevance.")
    url: str = Field(default="", description="The source URL of the content.")
    model_config = ConfigDict(extra='allow')

class MetadataExtractorArgs(BaseModel):
    content: str = Field(..., description="The content from which to extract metadata.")
    content_type: str = Field(default="text", description="The type of the content (e.g., 'text', 'image').")
    model_config = ConfigDict(extra='allow')

class CategorizationArgs(BaseModel):
    content: str = Field(..., description="The content to be categorized.")
    content_type: str = Field(default="textual", description="The primary type of the content.")
    model_config = ConfigDict(extra='allow')

# --------------------
# --- Custom Tools ---
# --------------------

class LlamaQualityAssessmentTool(BaseTool):
    name: str = "LlamaQualityAssessment"
    description: str = "Assesses the quality of fashion content for AI training suitability."
    args_schema: Type[BaseModel] = QualityAssessmentArgs

    def _run(self, content: str, **kwargs) -> Dict[str, Any]:
        # Placeholder for an actual LLM call.
        assessment_result = {
            "fashion_llm_score": 7.8,
            "overall_quality": 8.2,
            "key_concepts": ["fashion trends", "clothing styles", "design elements"],
            "training_suitability": "Excellent",
            "content_length": len(content),
            "assessed_at": datetime.now().isoformat()
        }
        self._save_assessment(assessment_result)
        return assessment_result

    def _save_assessment(self, assessment: Dict[str, Any]):
        data_dir = Path("C:/Users/saran/Videos/Projects/FDM/dataCollector/metadata")
        data_dir.mkdir(parents=True, exist_ok=True)
        timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
        filename = data_dir / f"quality_assessment_{timestamp}.json"
        with open(filename, "w", encoding="utf-8") as f:
            json.dump(assessment, f, indent=2)
        print(f"✅ Assessment saved to: {filename}")

class LlamaRelevanceFilterTool(BaseTool):
    name: str = "LlamaRelevanceFilter"
    description: str = "Filters content based on its relevance to the fashion domain."
    args_schema: Type[BaseModel] = RelevanceFilterArgs

    def _run(self, content: str, url: str = "", **kwargs) -> Dict[str, Any]:
        fashion_keywords = ["fashion", "clothing", "apparel", "style", "design", "fabric", "textile", "garment", "outfit", "trend"]
        content_lower = content.lower()
        found_keywords = [kw for kw in fashion_keywords if kw in content_lower]
        
        return {
            "is_relevant": len(found_keywords) > 0,
            "confidence": min(0.95, len(found_keywords) * 0.15),
            "keywords_found": found_keywords,
            "url": url,
            "processed_at": datetime.now().isoformat()
        }

class FashionMetadataExtractor(BaseTool):
    name: str = "FashionMetadataExtractor"
    description: str = "Extracts structured metadata from fashion-related content."
    args_schema: Type[BaseModel] = MetadataExtractorArgs

    def _run(self, content: str, content_type: str = "text", **kwargs) -> Dict[str, Any]:
        if content.strip().startswith('{'):
            try:
                content_data = json.loads(content)
            except json.JSONDecodeError:
                content_data = {"text": content}
        else:
            content_data = {"text": content}
        
        metadata = {
            "url": content_data.get("url", "N/A"),
            "title": content_data.get("title", "N/A"),
            "word_count": len(content_data.get("text", "").split()),
            "content_type": content_type,
            "has_images": bool(content_data.get("images")),
            "extracted_at": datetime.now().isoformat()
        }
        self._save_metadata(metadata)
        return metadata

    def _save_metadata(self, metadata: Dict[str, Any]):
        data_dir = Path("C:/Users/saran/Videos/Projects/FDM/dataCollector/metadata")
        data_dir.mkdir(parents=True, exist_ok=True)
        timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
        filename = data_dir / f"metadata_{timestamp}.json"
        with open(filename, "w", encoding="utf-8") as f:
            json.dump(metadata, f, indent=2)
        print(f"✅ Metadata saved to: {filename}")

class LlamaCategorizationTool(BaseTool):
    name: str = "LlamaCategorizationTool"
    description: str = "Categorizes fashion content into the correct folder structure."
    args_schema: Type[BaseModel] = CategorizationArgs

    def _run(self, content: str, content_type: str = "textual", **kwargs) -> Dict[str, Any]:
        content_lower = content.lower()
        
        if any(kw in content_lower for kw in ["trend", "forecast", "season"]):
            category, subcategory = "textual", "trend_reports"
        elif any(kw in content_lower for kw in ["theory", "principle", "concept"]):
            category, subcategory = "textual", "fashion_theory"
        elif any(kw in content_lower for kw in ["review", "rating", "opinion"]):
            category, subcategory = "textual", "consumer_feedback"
        else:
            category, subcategory = "textual", "fashion_theory"
        
        result = {
            "category": category,
            "subcategory": subcategory,
            "confidence": 0.85,
            "reasoning": f"Content classified as '{subcategory}' based on keyword matching.",
            "processed_at": datetime.now().isoformat()
        }
        self._save_categorization(result)
        return result

    def _save_categorization(self, result: Dict[str, Any]):
        data_dir = Path("C:/Users/saran/Videos/Projects/FDM/dataCollector/metadata")
        data_dir.mkdir(parents=True, exist_ok=True)
        timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
        filename = data_dir / f"categorization_{timestamp}.json"
        with open(filename, "w", encoding="utf-8") as f:
            json.dump(result, f, indent=2)
        print(f"✅ Categorization saved to: {filename}")

# ------------------------------------
# --- Tool Collection ---
# ------------------------------------

class FashionDataPipelineTools:
    """A class to encapsulate and provide all data processing tools."""
    def __init__(self):
        self.llama_quality_assessment = LlamaQualityAssessmentTool()
        self.llama_relevance_filter = LlamaRelevanceFilterTool()
        self.fashion_metadata_extractor = FashionMetadataExtractor()
        self.llama_categorization = LlamaCategorizationTool()

    def get_all_tools(self) -> list:
        """Returns a list of all tool instances for easy agent integration."""
        return [
            self.llama_quality_assessment,
            self.llama_relevance_filter,
            self.fashion_metadata_extractor,
            self.llama_categorization
        ]
</file>

<file path="src/scrapper/tools/metadata_tools.py">
from pydantic import BaseModel, Field, ConfigDict
from crewai.tools import BaseTool
from typing import Type, Dict, Any
from datetime import datetime
import json
from pathlib import Path

class MetadataExtractorArgs(BaseModel):
    content: Any = Field(..., description="Content to extract metadata from")
    content_type: str = Field(default='text', description='Type of content')
    model_config = ConfigDict(extra='forbid')

class FashionMetadataExtractor(BaseTool):
    name: str = 'FashionMetadataExtractor'
    description: str = 'Extract structured metadata from fashion content'
    args_schema: Type[BaseModel] = MetadataExtractorArgs

    def _run(self, content: Any = None, content_type: str = 'text') -> Dict[str, Any]:
        if isinstance(content, str):
            try:
                content = json.loads(content)
            except json.JSONDecodeError:
                return {"error": "Invalid content format; expected a JSON dictionary string."}

        if not isinstance(content, dict):
            return {"error": "Content must be a dictionary or JSON string"}

        metadata = {
            "url": content.get("url", "N/A"),
            "title": content.get("title", "N/A"), 
            "word_count": len(content.get("text", "").split()),
            "content_type": content_type,
            "extracted_at": datetime.now().isoformat()
        }

        # Save metadata to file
        try:
            data_dir = Path("C:/Users/saran/Videos/Projects/FDM/dataCollector/metadata")
            data_dir.mkdir(parents=True, exist_ok=True)
            timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
            filename = f"metadata_{timestamp}.json"
            with open(data_dir / filename, "w", encoding="utf-8") as f:
                json.dump(metadata, f, indent=2)
            print(f"✅ Metadata saved to: {data_dir / filename}")
        except Exception as e:
            print(f"❌ Failed to save metadata: {e}")

        return metadata
</file>

<file path="src/scrapper/tools/scrape_tools.py">
# scrapper/tools/scrape_tools.py

from pydantic import BaseModel, Field, ConfigDict
from crewai.tools import BaseTool
from typing import Type, List, Dict, Any
from datetime import datetime
from pathlib import Path
import requests
from bs4 import BeautifulSoup
# FIX: Import the json module to handle string-to-list conversion
import json

# ------------------------------
# --- Pydantic Argument Models ---
# ------------------------------

class BatchScrapingArgs(BaseModel):
    """Arguments for the BatchScrapingTool."""
    urls: List[str] = Field(..., description="A list of URLs to be scraped for content.")
    max_content_length: int = Field(default=10000, description="The maximum character length for the scraped content from each URL.")
    
    # This configuration is correct and allows the tool to work with CrewAI.
    model_config = ConfigDict(extra="allow")

# --------------------
# --- Custom Tools ---
# --------------------

class BatchScrapingTool(BaseTool):
    """A tool to scrape the textual content from a list of fashion websites."""
    name: str = "BatchScraping"
    description: str = "Scrapes the primary text content from a list of multiple fashion websites."
    args_schema: Type[BaseModel] = BatchScrapingArgs

    def _run(self, urls: List[str] | str, max_content_length: int = 10000, **kwargs) -> Dict[str, Any]:
        """
        Executes the batch scraping of URLs.
        
        This method is designed to be robust and can accept the 'urls'
        argument as either a proper list of strings or a JSON-formatted string
        that represents a list of strings.
        """
        
        # --- FIX: Robust URL input handling ---
        if isinstance(urls, str):
            try:
                # If 'urls' is a string, attempt to parse it as JSON.
                # This handles cases where the LLM or a previous tool returns a stringified list.
                parsed_urls = json.loads(urls)
                if not isinstance(parsed_urls, list):
                    return {"error": "Input string was valid JSON but not a list."}
                urls = parsed_urls
            except json.JSONDecodeError:
                # If parsing fails, it might be a single URL string.
                # Wrap it in a list to handle this case gracefully.
                urls = [urls]
        
        if not isinstance(urls, list):
            return {"error": f"Invalid type for 'urls'. Expected a list or a JSON string of a list, but got {type(urls).__name__}."}
        # --- End of FIX ---

        scraped_content = []
        failed_urls = []

        for url in urls[:10]:  # Limit to 10 URLs per batch for safety
            try:
                headers = {
                    'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/91.0.4472.124 Safari/537.36'
                }
                response = requests.get(url, timeout=15, headers=headers)
                response.raise_for_status()
                
                soup = BeautifulSoup(response.content, 'html.parser')
                
                for script_or_style in soup(["script", "style"]):
                    script_or_style.decompose()
                
                text = ' '.join(line.strip() for line in soup.get_text().splitlines() if line.strip())
                
                if len(text) > max_content_length:
                    text = text[:max_content_length] + "..."

                scraped_content.append({
                    "url": url,
                    "title": soup.title.string.strip() if soup.title and soup.title.string else "No Title Found",
                    "content": text,
                    "scraped_at": datetime.now().isoformat()
                })

            except Exception as e:
                failed_urls.append({"url": url, "error": str(e)})

        return {
            "success": True,
            "scraped_content": scraped_content,
            "failed_urls": failed_urls
        }
</file>

<file path="src/scrapper/tools/search_tools.py">
# scrapper/tools/search_tools.py

from pydantic import BaseModel, Field, ConfigDict
from crewai.tools import BaseTool
from typing import Type, Dict, Any, List, Optional
from ddgs import DDGS
from scrapper.config import FASHION_KEYWORDS

class URLDiscoveryArgs(BaseModel):
    """Arguments for the FashionURLDiscoveryTool."""
    query: str = Field(..., description="Search query for fashion URLs")
    max_results: int = Field(default=10, description="Maximum results to return")
    model_config = ConfigDict(extra="allow")

class FashionURLDiscoveryTool(BaseTool):
    # FIX: Added ': str' type annotation to override BaseTool fields correctly
    name: str = "FashionURLDiscovery"
    description: str = "Discover fashion-related URLs based on a query or predefined keywords."
    args_schema: Type[BaseModel] = URLDiscoveryArgs

    def _run(self, query: str, max_results: int = 10, **kwargs) -> Dict[str, Any]:
        search_terms = [query] if query else (kwargs.get("keywords", []))
        
        all_urls = []
        try:
            with DDGS() as ddgs:
                for term in search_terms[:5]:
                    if not term: continue
                    results = ddgs.text(term, max_results=max_results)
                    for result in results:
                        all_urls.append({
                            "title": result.get("title", ""),
                            "url": result.get("href", ""),
                            "snippet": result.get("body", ""),
                            "source": "duckduckgo",
                            "search_keyword": term
                        })
            return {"success": True, "urls": all_urls}
        except Exception as e:
            return {"success": False, "error": str(e)}

class URLValidatorArgs(BaseModel):
    """Arguments for the URLValidatorTool."""
    urls: List[Dict[str, Any]] = Field(..., description="A list of URL data dictionaries to validate.")
    model_config = ConfigDict(extra="allow")

class URLValidatorTool(BaseTool):
    # FIX: Added ': str' type annotation
    name: str = "URLValidator"
    description: str = "Validate a list of URLs for their relevance to fashion."
    args_schema: Type[BaseModel] = URLValidatorArgs

    def _run(self, urls: List[Dict[str, Any]], **kwargs) -> Dict[str, Any]:
        valid_urls, invalid_urls = [], []
        fashion_keywords = {kw for cat in FASHION_KEYWORDS.values() for kw in cat}

        for url_data in urls:
            try:
                url = url_data.get("url", "")
                title = url_data.get("title", "").lower()
                snippet = url_data.get("snippet", "").lower()
                
                if not url or not url.startswith(("http://", "https://")):
                    invalid_urls.append({"url": url, "reason": "Invalid URL format"})
                    continue
                
                if any(keyword in f"{title} {snippet}" for keyword in fashion_keywords):
                    valid_urls.append(url_data)
                else:
                    invalid_urls.append({"url": url, "reason": "Low relevance to fashion."})
            except Exception as e:
                invalid_urls.append({"url": url_data.get("url", ""), "reason": f"Validation error: {e}"})
        
        return {"success": True, "valid_urls": valid_urls, "invalid_urls": invalid_urls}


def validate_fashion_url(url, title, snippet):
    fashion_keywords = [
        "fashion", "style", "clothing", "outfit", "trend", "runway", 
        "designer", "apparel", "garment", "dress", "shoes", "accessories"
    ]
    
    content = f"{title} {snippet}".lower()
    keyword_matches = sum(1 for keyword in fashion_keywords if keyword in content)
    
    # More lenient validation
    is_relevant = keyword_matches >= 1
    confidence = min(keyword_matches / len(fashion_keywords), 1.0)
    
    return {
        "is_relevant": is_relevant,
        "confidence": confidence,
        "keywords_found": [kw for kw in fashion_keywords if kw in content]
    }
</file>

<file path="src/scrapper/tools/url_validator.py">
class URLValidator:
    def __init__(self):
        self.fashion_keywords = [
            "fashion", "style", "clothing", "outfit", "trend", "runway",
            "designer", "apparel", "garment", "dress", "shoes", "accessories",
            "vogue", "elle", "harper", "bazaar", "glamour", "beauty"
        ]
    
    def validate_urls(self, urls):
        valid_urls = []
        invalid_urls = []
        
        for url_data in urls:
            url = url_data.get("url", "")
            title = url_data.get("title", "")
            snippet = url_data.get("snippet", "")
            
            # Check content for fashion keywords
            content = f"{title} {snippet} {url}".lower()
            keyword_matches = [kw for kw in self.fashion_keywords if kw in content]
            
            # More lenient validation - need at least 1 keyword
            if len(keyword_matches) >= 1:
                valid_urls.append({
                    "url": url,
                    "title": title,
                    "snippet": snippet,
                    "relevance_score": len(keyword_matches) / len(self.fashion_keywords),
                    "keywords_found": keyword_matches
                })
            else:
                invalid_urls.append({
                    "url": url,
                    "reason": f"No fashion keywords found. Content: {content[:100]}..."
                })
        
        return {
            "success": True,
            "valid_urls": valid_urls,
            "invalid_urls": invalid_urls
        }
</file>

<file path="src/scrapper/utils/data_export.py">
def save_scraped_data_to_tsv(crew_results, filename):
    """Export data in AI-training-ready format - with format detection"""
    
    import json
    import csv
    import os
    import glob
    from datetime import datetime
    from PIL import Image
    import gzip
    import numpy as np
    
    # DEBUG: Print the crew results structure first
    print("🔍 DEBUG: Crew results structure:")
    if isinstance(crew_results, dict):
        print(f"   Keys: {list(crew_results.keys())}")
        for key, value in crew_results.items():
            print(f"   {key}: {type(value)} - {len(str(value)[:100])} chars")
    else:
        print(f"   Type: {type(crew_results)}")
        print(f"   Value: {str(crew_results)[:200]}...")
    
    training_data = []
    
    # Create training directories
    os.makedirs("dataCollector/training_images", exist_ok=True)
    os.makedirs("dataCollector/training_text", exist_ok=True)
    
    # Process Fashion-MNIST for Visual Generation Model
    fashion_mnist_path = "dataCollector/structured/fashion_mnist/"
    if os.path.exists(fashion_mnist_path):
        print("🖼️ Processing Fashion-MNIST for AI training...")
        
        try:
            # Extract Fashion-MNIST images
            with gzip.open(f"{fashion_mnist_path}/train-images-idx3-ubyte.gz", 'rb') as f:
                f.read(16)  # Skip header
                images = np.frombuffer(f.read(), np.uint8).reshape(-1, 28, 28)
            
            with gzip.open(f"{fashion_mnist_path}/train-labels-idx1-ubyte.gz", 'rb') as f:
                f.read(8)  # Skip header  
                labels = np.frombuffer(f.read(), np.uint8)
            
            class_names = ['T-shirt', 'Trouser', 'Pullover', 'Dress', 'Coat',
                          'Sandal', 'Shirt', 'Sneaker', 'Bag', 'Ankle boot']
            
            # Process first 1000 images for AI training
            for i in range(min(1000, len(images))):
                img = Image.fromarray(images[i], mode='L')
                # Resize to standard ControlNet size
                img_resized = img.resize((512, 512), Image.Resampling.LANCZOS)
                
                filename_img = f"dataCollector/training_images/fashion_mnist_{i:05d}_{class_names[labels[i]].replace('/', '_')}.png"
                img_resized.save(filename_img)
                
                training_data.append({
                    'file_path': filename_img,
                    'data_type': 'image',
                    'ai_model_target': 'visual_gen',
                    'resolution': '512x512',
                    'format': 'PNG',
                    'quality_score': 9.0,
                    'category': 'garment',
                    'subcategory': class_names[labels[i]].lower().replace('/', '_'),
                    'text_content': f"Fashion garment: {class_names[labels[i]]}",
                    'metadata_json': json.dumps({
                        'garment_type': class_names[labels[i]],
                        'color_mode': 'grayscale',
                        'source': 'fashion_mnist',
                        'training_split': 'train' if i < 800 else 'validation',
                        'original_size': '28x28',
                        'upscaled': True
                    }),
                    'training_ready': True,
                    'created_at': datetime.now().isoformat()
                })
                
            print(f"✅ Processed {min(1000, len(images))} Fashion-MNIST images")
        except Exception as e:
            print(f"⚠️ Fashion-MNIST processing error: {e}")
    
    # Process scraped content with flexible format detection
    scraped_content = []
    
    # Try different possible structures
    if isinstance(crew_results, dict):
        # Try common CrewAI result structures
        possible_keys = ['scraped_content', 'result', 'output', 'data', 'content', 'tasks_output']
        
        for key in possible_keys:
            if key in crew_results:
                content = crew_results[key]
                print(f"📝 Found scraped content in key: '{key}'")
                
                if isinstance(content, list):
                    scraped_content = content
                    break
                elif isinstance(content, str):
                    # Try to parse as JSON
                    try:
                        parsed = json.loads(content)
                        if isinstance(parsed, list):
                            scraped_content = parsed
                            break
                    except:
                        # Treat as single text content
                        scraped_content = [{'content': content, 'quality_score': 8.0}]
                        break
                elif isinstance(content, dict) and 'items' in content:
                    scraped_content = content['items']
                    break
    
    # Process any scraped content found
    if scraped_content:
        print(f"📝 Processing {len(scraped_content)} scraped items for LLM training...")
        
        for i, item in enumerate(scraped_content):
            # Handle different item structures
            if isinstance(item, str):
                item = {'content': item, 'quality_score': 7.0}
            elif not isinstance(item, dict):
                continue
                
            content_text = item.get('content', str(item))
            quality = item.get('quality_score', item.get('fashion_llm_score', 7.0))
            
            if isinstance(quality, (int, float)) and quality >= 7.0 and len(content_text) > 100:
                # Save text content for LLM fine-tuning
                text_filename = f"dataCollector/training_text/fashion_scraped_{i:05d}.txt"
                
                try:
                    with open(text_filename, 'w', encoding='utf-8') as f:
                        f.write(content_text)
                    
                    content_preview = content_text[:200] + "..." if len(content_text) > 200 else content_text
                    
                    training_data.append({
                        'file_path': text_filename,
                        'data_type': 'text',
                        'ai_model_target': 'fashion_llm',
                        'resolution': 'N/A',
                        'format': 'TXT',
                        'quality_score': quality,
                        'category': 'fashion_theory',
                        'subcategory': item.get('category', 'scraped_content'),
                        'text_content': content_preview,
                        'metadata_json': json.dumps({
                            'word_count': len(content_text.split()),
                            'source_url': item.get('url', 'crew_scraped'),
                            'scrape_date': datetime.now().isoformat(),
                            'crew_agent': item.get('agent', 'unknown')
                        }),
                        'training_ready': True,
                        'created_at': datetime.now().isoformat()
                    })
                except Exception as e:
                    print(f"⚠️ Error processing text content {i}: {e}")
    
    # Export AI-training-ready TSV
    if training_data:
        fieldnames = ['file_path', 'data_type', 'ai_model_target', 'resolution', 
                     'format', 'quality_score', 'category', 'subcategory', 
                     'text_content', 'metadata_json', 'training_ready', 'created_at']
        
        with open(filename, 'w', newline='', encoding='utf-8') as f:
            writer = csv.DictWriter(f, fieldnames=fieldnames, delimiter='\t')
            writer.writeheader()
            writer.writerows(training_data)
        
        print(f"✅ AI-training-ready TSV saved: {filename}")
        print(f"📊 Total training records: {len(training_data)}")
        
        # Print summary by AI model target
        summary = {}
        for item in training_data:
            target = item['ai_model_target']
            summary[target] = summary.get(target, 0) + 1
        
        print("🎯 Training data breakdown:")
        for target, count in summary.items():
            print(f"   - {target}: {count} records")
            
        return filename, len(training_data)
    
    else:
        print("⚠️ No training data generated - check crew results structure above")
        return filename, 0
</file>

<file path="src/scrapper/utils/duplicate_checker.py">
import json
from pathlib import Path

class DuplicateChecker:
    def __init__(self, tracking_file: Path):
        self.tracking_file = tracking_file
        self.content_hashes = set()
        self.load_hashes()

    def load_hashes(self):
        if self.tracking_file.exists():
            with open(self.tracking_file) as f:
                hashes = json.load(f)
                self.content_hashes = set(hashes)

    def is_duplicate(self, content_hash: str):
        return content_hash in self.content_hashes

    def add_hash(self, content_hash: str):
        self.content_hashes.add(content_hash)
        with open(self.tracking_file, "w") as f:
            json.dump(list(self.content_hashes), f)
</file>

<file path="src/scrapper/crew.py">
# Optional: implement specialized crew orchestrations here if needed
# Example: Advanced error handling, parallelization control, etc.

def custom_crew_orchestration():
    pass
</file>

<file path="src/scrapper/main.py">
# scrapper/main.py
# type: ignore
import json
import logging
import os
import requests
import numpy as np
from datetime import datetime
from pathlib import Path
from typing import List, Dict, Any

import yaml
from crewai import Agent, Task, Crew, Process
from crewai.tools import BaseTool
from crewai.llm import LLM

# Local imports
from scrapper.config import create_directories, OLLAMA_BASE_URL
from scrapper.tools.llama_tools import (
    LlamaQualityAssessmentTool,
    LlamaCategorizationTool,
    LlamaRelevanceFilterTool,
    FashionMetadataExtractor,
)
from scrapper.tools.search_tools import FashionURLDiscoveryTool, URLValidatorTool
from scrapper.tools.scrape_tools import BatchScrapingTool
from scrapper.tools.dataset_tools import KaggleDatasetTool, HuggingFaceDatasetTool
from scrapper.tools.image_processing_tools import PhotoToSketchTool
from scrapper.utils.data_export import save_scraped_data_to_tsv

# Setup logging
logging.basicConfig(
    filename="scraper_activity.log",
    level=logging.INFO,
    format="%(asctime)s - %(levelname)s - %(message)s",
)


def load_yaml_config(filename: str) -> Dict[str, Any]:
    """Load YAML config from the config subdirectory."""
    config_dir = Path(__file__).parent / "config"
    file_path = config_dir / filename
    try:
        with open(file_path, "r", encoding="utf-8") as f:
            return yaml.safe_load(f)
    except Exception as e:
        logging.error(f"Error loading YAML '{filename}': {e}")
        raise


def try_fallback_downloads():
    """Fallback method if API downloads fail"""
    base_dir = "C:/Users/saran/Videos/Projects/FDM/dataCollector/structured"
    
    print("🔄 Attempting fallback dataset downloads...")
    
    # Try direct Fashion-MNIST download
    try:
        download_fashion_mnist_direct(base_dir)
    except Exception as e:
        print(f"⚠️ Fallback Fashion-MNIST download failed: {e}")
        logging.error(f"Fallback download failed: {e}")
    
    # Try alternative HuggingFace approach
    try:
        download_fashion_mnist_hf_fallback(base_dir)
    except Exception as e:
        print(f"⚠️ Fallback HuggingFace download failed: {e}")
        logging.error(f"Fallback HF download failed: {e}")


def download_fashion_mnist_direct(base_dir: str):
    """Direct download without Kaggle API"""
    print("🔽 Trying direct Fashion-MNIST download...")
    
    base_url = "http://fashion-mnist.s3-website.eu-central-1.amazonaws.com/"
    files = [
        "train-images-idx3-ubyte.gz",
        "train-labels-idx1-ubyte.gz", 
        "t10k-images-idx3-ubyte.gz",
        "t10k-labels-idx1-ubyte.gz"
    ]
    
    os.makedirs(f"{base_dir}/fashion_mnist", exist_ok=True)
    
    success_count = 0
    for file in files:
        try:
            url = base_url + file
            response = requests.get(url, timeout=30)
            if response.status_code == 200:
                with open(f"{base_dir}/fashion_mnist/{file}", "wb") as f:
                    f.write(response.content)
                print(f"✅ Downloaded: {file}")
                success_count += 1
            else:
                print(f"❌ Failed to download: {file} (Status: {response.status_code})")
        except Exception as e:
            print(f"❌ Error downloading {file}: {e}")
    
    if success_count > 0:
        print(f"✅ Successfully downloaded {success_count}/{len(files)} Fashion-MNIST files")
    else:
        raise Exception("No files were downloaded successfully")


def download_fashion_mnist_hf_fallback(base_dir: str):
    """Alternative HuggingFace download with proper serialization"""
    print("🔽 Trying HuggingFace Fashion-MNIST fallback...")
    
    try:
        from datasets import load_dataset
        
        dataset = load_dataset("fashion_mnist")
        
        os.makedirs(f"{base_dir}/fashion_mnist_hf", exist_ok=True)
        
        # Process train split (limited samples to avoid memory issues)
        max_samples = 1000
        train_images = []
        train_labels = []
        
        for i, item in enumerate(dataset["train"]): 
            if i >= max_samples:
                break
                
            # Convert PIL Image to numpy array then to list for JSON serialization
            img_array = np.array(item["image"])
            train_images.append(img_array.tolist())
            train_labels.append(item["label"])
        
        # Save as JSON files
        with open(f"{base_dir}/fashion_mnist_hf/train_images.json", "w") as f:
            json.dump(train_images, f)
        
        with open(f"{base_dir}/fashion_mnist_hf/train_labels.json", "w") as f:
            json.dump(train_labels, f)
        
        # Process test split
        test_images = []
        test_labels = []
        
        for i, item in enumerate(dataset["test"]):
            if i >= max_samples:
                break
                
            img_array = np.array(item["image"])
            test_images.append(img_array.tolist())
            test_labels.append(item["label"])
        
        with open(f"{base_dir}/fashion_mnist_hf/test_images.json", "w") as f:
            json.dump(test_images, f)
        
        with open(f"{base_dir}/fashion_mnist_hf/test_labels.json", "w") as f:
            json.dump(test_labels, f)
            
        print(f"✅ Successfully downloaded {len(train_images)} train + {len(test_images)} test samples from HuggingFace")
        
    except ImportError:
        print("⚠️ HuggingFace datasets library not installed")
        raise Exception("datasets library not available")


def create_fashion_crew():
    """Create and configure the fashion data collection crew."""
    # Load configurations
    agents_config = load_yaml_config("agents.yaml")
    tasks_config = load_yaml_config("tasks.yaml")

    # --- LLM Configuration with Increased Timeout ---
    ollama_llm = LLM(
        model="ollama/llama3.1:8b",
        base_url=OLLAMA_BASE_URL,
        request_timeout=1200.0,  # 20 minutes timeout
    )

    # Instantiate all tools
    discovery_tools: List[BaseTool] = [FashionURLDiscoveryTool(), URLValidatorTool()]
    scraping_tools: List[BaseTool] = [BatchScrapingTool(), PhotoToSketchTool()]
    quality_tools: List[BaseTool] = [
        LlamaQualityAssessmentTool(),
        LlamaRelevanceFilterTool(),
        FashionMetadataExtractor(),
    ]
    categorization_tools: List[BaseTool] = [LlamaCategorizationTool()]
    dataset_tools: List[BaseTool] = [KaggleDatasetTool(), HuggingFaceDatasetTool()]

    # --- Agent Definitions ---
    discovery_agent = Agent(
        role=agents_config["discovery_specialist"]["role"],
        goal=agents_config["discovery_specialist"]["goal"],
        backstory=agents_config["discovery_specialist"]["backstory"],
        tools=discovery_tools,
        llm=ollama_llm,
        verbose=True,
        allow_delegation=False,
    )

    scraping_agent = Agent(
        role=agents_config["scraping_specialist"]["role"],
        goal=agents_config["scraping_specialist"]["goal"],
        backstory=agents_config["scraping_specialist"]["backstory"],
        tools=scraping_tools,
        llm=ollama_llm,
        verbose=True,
        allow_delegation=False,
    )

    quality_agent = Agent(
        role=agents_config["quality_assessor"]["role"],
        goal=agents_config["quality_assessor"]["goal"],
        backstory=agents_config["quality_assessor"]["backstory"],
        tools=quality_tools,
        llm=ollama_llm,
        verbose=True,
        allow_delegation=False,
    )

    categorization_agent = Agent(
        role=agents_config["content_categorizer"]["role"],
        goal=agents_config["content_categorizer"]["goal"],
        backstory=agents_config["content_categorizer"]["backstory"],
        tools=categorization_tools,
        llm=ollama_llm,
        verbose=True,
        allow_delegation=False,
    )

    dataset_agent = Agent(
        role=agents_config["dataset_specialist"]["role"],
        goal=agents_config["dataset_specialist"]["goal"],
        backstory=agents_config["dataset_specialist"]["backstory"],
        tools=dataset_tools,
        llm=ollama_llm,
        verbose=True,
        allow_delegation=False,
    )

    # --- Task Definitions ---
    discovery_task = Task(
        description=tasks_config["url_discovery"]["description"],
        agent=discovery_agent,
        expected_output=tasks_config["url_discovery"]["expected_output"],
    )
    
    scraping_task = Task(
        description=tasks_config["content_scraping"]["description"],
        agent=scraping_agent,
        expected_output=tasks_config["content_scraping"]["expected_output"],
    )
    
    quality_task = Task(
        description=tasks_config["quality_assessment"]["description"],
        agent=quality_agent,
        expected_output=tasks_config["quality_assessment"]["expected_output"],
    )
    
    categorization_task = Task(
        description=tasks_config["content_categorization"]["description"],
        agent=categorization_agent,
        expected_output=tasks_config["content_categorization"]["expected_output"],
    )
    
    dataset_collection_task = Task(
        description=tasks_config["dataset_collection"]["description"],
        agent=dataset_agent,
        expected_output=tasks_config["dataset_collection"]["expected_output"],
    )

    # Assemble the crew
    fashion_crew = Crew(
        agents=[
            discovery_agent,
            scraping_agent,
            quality_agent,
            categorization_agent,
            dataset_agent,
        ],
        tasks=[
            discovery_task,
            scraping_task,
            quality_task,
            categorization_task,
            dataset_collection_task,
        ],
        process=Process.sequential,
        verbose=True,
    )
    
    return fashion_crew


def main():
    """Main function to initialize and run the fashion data collection crew."""
    print("🚀 Starting Fashion Data Collection Crew...")
    
    # Create directories and setup
    create_directories()
    logging.info(f"Connecting to LLaMA at: {OLLAMA_BASE_URL}")

    # Try fallback downloads first (before crew execution)
    try_fallback_downloads()

    # Create and run the crew
    try:
        fashion_crew = create_fashion_crew()
        
        logging.info("Crew kickoff started.")
        print("🎬 Starting crew execution...")
        
        result = fashion_crew.kickoff()
        
        print("✅ Crew execution completed!")

        # --- Save Final Results ---
        output_dir = Path("C:/Users/saran/Videos/Projects/FDM/dataCollector/results")
        output_dir.mkdir(parents=True, exist_ok=True)
        timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")

        # 1. Save JSON
        json_filename = output_dir / f"crew_results_{timestamp}.json"
        try:
            with open(json_filename, "w", encoding="utf-8") as f:
                json.dump(result, f, indent=2, default=str)
            print(f"🎯 Raw JSON results saved to: {json_filename}")
        except Exception as e:
            logging.error(f"Failed to save JSON: {e}")
            print(f"❌ Failed to save JSON: {e}")

        # 2. Save TSV
        tsv_output_dir = Path(
            "C:/Users/saran/Videos/Projects/FDM/dataCollector/collected_data/structured/market_data"
        )
        tsv_filename = tsv_output_dir / f"fashion_data_export_{timestamp}.tsv"
        try:
            if isinstance(result, dict):
                tsv_file, record_count = save_scraped_data_to_tsv(result, tsv_filename)
                print(f"🎯 Generated {record_count} AI-ready training records")
            else:
                logging.warning("Crew result was not a dictionary, cannot save to TSV.")
                print("⚠️ Crew result format unexpected, TSV not saved.")
        except Exception as e:
            logging.error(f"Failed to save TSV: {e}")
            print(f"❌ Failed to save TSV: {e}")

        print("🎉 Fashion data collection completed successfully!")
        return result

    except Exception as e:
        logging.error(f"Crew execution failed: {e}")
        print(f"❌ Crew execution failed: {e}")
        print("💡 Check logs for more details and ensure Ollama is running.")
        return None


if __name__ == "__main__":
    main()
</file>

<file path="src/fashion_data_20250902_001145.tsv">
url	title	text	word_count	source	scraped_at
https://www.google.com/search?q=fashion+clothing	Google Search	Google Search Please click here if you are not redirected within a few seconds. If you're having trouble accessing Google Search, please click here , or send feedback .	29	unknown	2025-09-02T00:11:40.918939
https://www.bing.com/search?q=designer+clothing	designer clothing - Search	designer clothing - Search Skip to content Mobile English हिंदी বাংলা اردو ਪੰਜਾਬੀ मराठी తెలుగు தமிழ் ಕನ್ನಡ ગુજરાતી മലയാളം ଓଡ଼ିଆ One last step Please solve the challenge below to continue	30	unknown	2025-09-02T00:11:43.208232
</file>

<file path="src/fashion_data_20250902_001150.tsv">
url	title	text	word_count	source	scraped_at
https://www.google.com/search?q=fashion+clothing	Google Search	Google Search Please click here if you are not redirected within a few seconds. If you're having trouble accessing Google Search, please click here , or send feedback .	29	unknown	2025-09-02T00:11:45.445783
https://www.bing.com/search?q=designer+clothing	designer clothing - Search	designer clothing - Search Skip to content Deep search Mobile English हिंदी বাংলা اردو ਪੰਜਾਬੀ मराठी తెలుగు தமிழ் ಕನ್ನಡ ગુજરાતી മലയാളം ଓଡ଼ିଆ One last step Please solve the challenge below to continue	32	unknown	2025-09-02T00:11:48.159181
</file>

<file path="src/recovery.py">
import json
from datetime import datetime
from scrapper.utils.data_export import save_scraped_data_to_tsv

# Manually process the existing crew results
crew_results_file = "C:\\Users\\saran\\Videos\\Projects\\FDM\\dataCollector\\results\\crew_results_20250903_102101.json"

# Load the crew results
with open(crew_results_file, 'r', encoding='utf-8') as f:
    crew_data = json.load(f)

# Generate TSV with the new flexible function
timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
tsv_filename = f"C:\\Users\\saran\\Videos\\Projects\\FDM\\dataCollector\\results\\fashion_training_{timestamp}.tsv"

# Process with debugging
tsv_file, record_count = save_scraped_data_to_tsv(crew_data, tsv_filename)
print(f"🎯 Generated {record_count} AI-ready training records")
</file>

<file path="!LOGFILE!">
C:\Users\saran\Videos\Projects\FDM\scrapper\modern_env\Scripts\python.exe: Error while finding module specification for 'scrapper.main' (ModuleNotFoundError: No module named 'scrapper')
</file>

<file path=".gitignore">
.env
__pycache__/
.DS_Store
</file>

<file path="pyproject.toml">
[project]
name = "scrapper"
version = "0.1.0"
description = "scrapper using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]>=0.175.0,<1.0.0"
]

[project.scripts]
scrapper = "scrapper.main:run"
run_crew = "scrapper.main:run"
train = "scrapper.main:train"
replay = "scrapper.main:replay"
test = "scrapper.main:test"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
</file>

<file path="README.md">
# Scrapper Crew

Welcome to the Scrapper Crew project, powered by [crewAI](https://crewai.com). This template is designed to help you set up a multi-agent AI system with ease, leveraging the powerful and flexible framework provided by crewAI. Our goal is to enable your agents to collaborate effectively on complex tasks, maximizing their collective intelligence and capabilities.

## Installation

Ensure you have Python >=3.10 <3.14 installed on your system. This project uses [UV](https://docs.astral.sh/uv/) for dependency management and package handling, offering a seamless setup and execution experience.

First, if you haven't already, install uv:

```bash
pip install uv
```

Next, navigate to your project directory and install the dependencies:

(Optional) Lock the dependencies and install them by using the CLI command:
```bash
crewai install
```
### Customizing

**Add your `OPENAI_API_KEY` into the `.env` file**

- Modify `src/scrapper/config/agents.yaml` to define your agents
- Modify `src/scrapper/config/tasks.yaml` to define your tasks
- Modify `src/scrapper/crew.py` to add your own logic, tools and specific args
- Modify `src/scrapper/main.py` to add custom inputs for your agents and tasks

## Running the Project

To kickstart your crew of AI agents and begin task execution, run this from the root folder of your project:

```bash
$ crewai run
```

This command initializes the scrapper Crew, assembling the agents and assigning them tasks as defined in your configuration.

This example, unmodified, will run the create a `report.md` file with the output of a research on LLMs in the root folder.

## Understanding Your Crew

The scrapper Crew is composed of multiple AI agents, each with unique roles, goals, and tools. These agents collaborate on a series of tasks, defined in `config/tasks.yaml`, leveraging their collective skills to achieve complex objectives. The `config/agents.yaml` file outlines the capabilities and configurations of each agent in your crew.

## Support

For support, questions, or feedback regarding the Scrapper Crew or crewAI.
- Visit our [documentation](https://docs.crewai.com)
- Reach out to us through our [GitHub repository](https://github.com/joaomdmoura/crewai)
- [Join our Discord](https://discord.com/invite/X4JWnZnxPb)
- [Chat with our docs](https://chatg.pt/DWjSBZn)

Let's create wonders together with the power and simplicity of crewAI.
</file>

<file path="requirements.txt">
crewai==0.175.0
crewai-tools==0.65.0
chromadb==0.5.23
embedchain>=0.1.114
pydantic>=2.4.2
requests==2.31.0
beautifulsoup4==4.12.2
ddgs==9.5.5
lxml>=6.0.0
selenium==4.15.0
newspaper3k==0.2.8
pandas>=2.0.0
numpy==1.26.4
python-dotenv==1.0.0
aiohttp>=3.9.1,<4.0.0
Pillow==10.1.0
opencv-python==4.8.1.78
imagehash==4.3.1
fake-useragent==1.4.0
httpx>=0.27.0
tenacity==8.2.3
nltk==3.8.1
textstat==0.7.3
python-magic-bin==0.4.14
webdriver-manager==3.8.6
schedule==1.2.0
rich==13.7.0
tqdm==4.66.1
datasets==2.19.0
fsspec==2024.3.1
kaggle==1.5.16
</file>

<file path="run_overnight.bat">
@echo off
setlocal enabledelayedexpansion

:: Path to project and virtual environment
echo ========================================
echo  Starting the Overnight Scrapper Script
echo ========================================
set PROJECT_DIR=C:\Users\saran\Videos\Projects\FDM\scrapper
set SRC_DIR=%PROJECT_DIR%\src
set VENV_DIR=%PROJECT_DIR%\modern_env

:: Activate virtual environment
echo Activating virtual environment from: %VENV_DIR%
call "%VENV_DIR%\Scripts\activate.bat"
if %ERRORLEVEL% neq 0 (
    echo Failed to activate virtual environment. Exiting...
    pause
    exit /b 1
)
echo Virtual environment activated.

:loop
echo ----------------------------------------
echo Switching to source directory: %SRC_DIR%
cd /d "%SRC_DIR%"

echo Running scrapper.main using Python...
python -m scrapper.main

if %ERRORLEVEL% neq 0 (
    echo Scrapper exited with error code %ERRORLEVEL%.
) else (
    echo Scrapper run completed successfully.
)

echo ⏳ Restarting in 10 seconds...
timeout /t 10 /nobreak >nul
echo Restarting scrapper...
goto loop
</file>

<file path="test.py">
import sys
import os
import json
import requests
import numpy as np
from pathlib import Path

# Add the scrapper directory to Python path
sys.path.append('C:/Users/saran/Videos/Projects/FDM/scrapper/src')

def test_direct_fashion_mnist_download():
    """Test direct Fashion-MNIST download"""
    print("🧪 Testing direct Fashion-MNIST download...")
    
    base_dir = "C:/Users/saran/Videos/Projects/FDM/dataCollector/structured"
    base_url = "http://fashion-mnist.s3-website.eu-central-1.amazonaws.com/"
    files = [
        "train-images-idx3-ubyte.gz",
        "train-labels-idx1-ubyte.gz", 
        "t10k-images-idx3-ubyte.gz",
        "t10k-labels-idx1-ubyte.gz"
    ]
    
    os.makedirs(f"{base_dir}/fashion_mnist", exist_ok=True)
    
    success_count = 0
    for file in files:
        try:
            url = base_url + file
            print(f"  Downloading: {url}")
            response = requests.get(url, timeout=30)
            if response.status_code == 200:
                with open(f"{base_dir}/fashion_mnist/{file}", "wb") as f:
                    f.write(response.content)
                print(f"  ✅ Downloaded: {file} ({len(response.content)} bytes)")
                success_count += 1
            else:
                print(f"  ❌ Failed: {file} (Status: {response.status_code})")
        except Exception as e:
            print(f"  ❌ Error downloading {file}: {e}")
    
    print(f"📊 Result: {success_count}/{len(files)} files downloaded successfully")
    return success_count > 0

def test_huggingface_download():
    """Test HuggingFace download with proper serialization"""
    print("🧪 Testing HuggingFace Fashion-MNIST download...")
    
    try:
        from datasets import load_dataset
        
        base_dir = "C:/Users/saran/Videos/Projects/FDM/dataCollector/structured"
        os.makedirs(f"{base_dir}/fashion_mnist_hf", exist_ok=True)
        
        print("  Loading dataset from HuggingFace...")
        dataset = load_dataset("fashion_mnist")
        
        # Process small sample
        max_samples = 10  # Small test sample
        train_images = []
        train_labels = []
        
        print(f"  Processing {max_samples} samples...")
        for i, item in enumerate(dataset["train"]):
            if i >= max_samples:
                break
                
            # Convert PIL Image to numpy array then to list
            img_array = np.array(item["image"])
            train_images.append(img_array.tolist())
            train_labels.append(item["label"])
        
        # Save test files
        with open(f"{base_dir}/fashion_mnist_hf/test_images.json", "w") as f:
            json.dump(train_images, f)
        
        with open(f"{base_dir}/fashion_mnist_hf/test_labels.json", "w") as f:
            json.dump(train_labels, f)
            
        print(f"  ✅ Successfully processed {len(train_images)} samples")
        print(f"  ✅ Files saved to: {base_dir}/fashion_mnist_hf/")
        return True
        
    except ImportError:
        print("  ❌ HuggingFace datasets library not installed")
        print("  💡 Install with: pip install datasets")
        return False
    except Exception as e:
        print(f"  ❌ HuggingFace download failed: {e}")
        return False

def test_url_validation():
    """Test URL validation logic"""
    print("🧪 Testing URL validation logic...")
    
    fashion_keywords = [
        "fashion", "style", "clothing", "outfit", "trend", "runway",
        "designer", "apparel", "garment", "dress", "shoes", "accessories",
        "vogue", "elle", "harper", "bazaar", "glamour", "beauty"
    ]
    
    test_urls = [
        {"url": "https://www.vogue.com/fashion", "title": "Vogue Fashion", "snippet": "Latest fashion trends"},
        {"url": "https://www.wired.com/tech", "title": "WIRED Tech", "snippet": "Technology news and reviews"},
        {"url": "https://www.elle.com/beauty", "title": "Elle Beauty", "snippet": "Beauty and skincare tips"}
    ]
    
    for url_data in test_urls:
        url = url_data.get("url", "")
        title = url_data.get("title", "")
        snippet = url_data.get("snippet", "")
        
        content = f"{title} {snippet} {url}".lower()
        keyword_matches = [kw for kw in fashion_keywords if kw in content]
        
        is_relevant = len(keyword_matches) >= 1
        confidence = len(keyword_matches) / len(fashion_keywords)
        
        status = "✅ VALID" if is_relevant else "❌ INVALID"
        print(f"  {status}: {url}")
        print(f"    Keywords found: {keyword_matches}")
        print(f"    Confidence: {confidence:.2f}")
        print()

def test_directory_structure():
    """Test if required directories exist"""
    print("🧪 Testing directory structure...")
    
    required_dirs = [
        "C:/Users/saran/Videos/Projects/FDM/dataCollector",
        "C:/Users/saran/Videos/Projects/FDM/dataCollector/structured",
        "C:/Users/saran/Videos/Projects/FDM/dataCollector/textual",
        "C:/Users/saran/Videos/Projects/FDM/dataCollector/visual",
        "C:/Users/saran/Videos/Projects/FDM/dataCollector/metadata",
        "C:/Users/saran/Videos/Projects/FDM/dataCollector/logs",
        "C:/Users/saran/Videos/Projects/FDM/dataCollector/results"
    ]
    
    for dir_path in required_dirs:
        if os.path.exists(dir_path):
            print(f"  ✅ {dir_path}")
        else:
            print(f"  ❌ {dir_path} (missing)")
            try:
                os.makedirs(dir_path, exist_ok=True)
                print(f"    ✅ Created: {dir_path}")
            except Exception as e:
                print(f"    ❌ Failed to create: {e}")

def main():
    """Run all tests"""
    print("🚀 Running Fashion Data Collection Tests")
    print("=" * 50)
    
    # Test 1: Directory structure
    test_directory_structure()
    print()
    
    # Test 2: URL validation
    test_url_validation()
    print()
    
    # Test 3: Direct download
    direct_success = test_direct_fashion_mnist_download()
    print()
    
    # Test 4: HuggingFace download
    hf_success = test_huggingface_download()
    print()
    
    # Summary
    print("📋 Test Summary:")
    print(f"  Direct Fashion-MNIST download: {'✅ PASS' if direct_success else '❌ FAIL'}")
    print(f"  HuggingFace download: {'✅ PASS' if hf_success else '❌ FAIL'}")
    print()
    
    if direct_success or hf_success:
        print("🎉 At least one dataset download method is working!")
    else:
        print("⚠️ Both dataset download methods failed. Check your internet connection.")

if __name__ == "__main__":
    main()
</file>

</files>
