[project]
name = "go-http-server"
version = "0.1.0"
description = "Go HTTP server with handlers"
language = "go"

[execution]
# Direct Go execution
command = "go run main.go"

# Or if you want to build first
# command = "go build -o server && ./server"

# Or just use entry
# entry = "main.go"
