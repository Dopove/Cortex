[project]
name = "rust-actix-web"
version = "0.1.0"
description = "Rust Actix web server"
language = "rust"

[execution]
# Using cargo
command = "cargo run"

# Or with release mode
# command = "cargo run --release"

# Entry doesn't work well for Rust, use command
